<form action="/2" method="POST">
  <h2>Все пробелы в нем заменить символом "_".</h2>

  <div class="input-field">
    <input type="text" name="text">
    <label>Текст</label>
  </div>

  <button type="submit" class="btn">Заменить</button>

  {{#if text}}
    <p>{{text}}</p>
  {{else}}
    <p>No text!</p>
  {{/if}}
</form>